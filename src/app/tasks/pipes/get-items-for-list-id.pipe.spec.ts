import { GetItemsForListIdPipe } from './get-items-for-list-id.pipe';

describe('GetItemsForListIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetItemsForListIdPipe();
    expect(pipe).toBeTruthy();
  });
});
